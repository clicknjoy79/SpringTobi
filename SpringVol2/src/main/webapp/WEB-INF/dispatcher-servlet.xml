<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">
    <context:component-scan base-package="springbook.learningtest.web.spring_el"/>
    <mvc:annotation-driven conversion-service="myConversionService"/>

    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/view/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <bean id="messageSource" class="org.springframework.context.support.ResourceBundleMessageSource">
        <property name="basename" value="web.messages"/>
    </bean>

    <!--
    프로젝트에 이미지를 넣고 jar 파일로 압축한 다음 클래스 패스에 추가한 경우에는
    해당 이미지 파일에 접근하기 위해서는 img 태그에서 src에 클래스 패스에 있는 FULL 이미지 경로를 넣어줘야 한다.(META-INF 제외)-->
    <mvc:resources mapping="/ui/**" location="classpath:/META-INF/ui/"/>

</beans>